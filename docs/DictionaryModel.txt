Dictionary Database Structure:

Fields:
  - state         ->  Boolean       (Phrase - StopWord)
  - name          ->  String        (Dictionary Name)
  - phrase        ->  String
  - similars      ->  Set<String>

- Primary Key:
  - Partition
    Compound Partition Key: (state, name)

  - Clustering
    One Cluster Key: (phrase)
